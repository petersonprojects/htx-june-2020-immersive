
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dishes</title>
</head>

<body>
    
    <% dishes.forEach(record => { %>
        
        <h1><%= record.name %> </h1>
        <h2><%= record.description %> </h2>
        <h3><%= record.price %> </h3>
        <img src="<%= record.imageurl %>" alt="<%= record.name %>" style="height:300px;width:300px;">

    <% }) %>

</body>

</html>